# Changelog

## Unreleased

- Update `@supportbridge/sdk` to 0.10.3 and keep the direct
  `SupportBridge.install(McpServer, options)` integration.
- Use the SDK's standard assistance-intent and revenue-signal catalogs and
  explicitly enable the offer card.
- Document that Pitch-Fork has no intermediate MCP adapter layer and add
  regression coverage for SDK registration cleanup.
